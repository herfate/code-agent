【输出要求】在任务仓库根目录（cwd）生成 `mind_map_change_analysis.md`（Markdown），结构必须与本文档一致。

【路径约定】
- 基线脑图（只读）：`测试脑图/*.json`
- 主输出文件：`mind_map_change_analysis.md`（相对 cwd）

---

# 测试脑图改动分析 — mind_map_change_analysis.md

| 字段 | 值 |
|------|-----|
| 需求来源 | `task_prompt.md` |
| 脑图目录 | `测试脑图/` |
| 文档版本 | v1 |
| 分析时间 | `{{analysis_time}}` |

---

## 分析摘要

> **一句话**：{{one_line_summary}}

```json
{
  "ready_for_maintain": false,
  "change_items_count": 0,
  "add_count": 0,
  "modify_count": 0,
  "delete_count": 0,
  "unchanged_count": 0,
  "blocking_gaps_count": 0,
  "non_blocking_gaps_count": 0,
  "summary_zh": "",
  "mind_map_files_scanned": [],
  "keywords_used": []
}
```

| 字段 | 说明 |
|------|------|
| `ready_for_maintain` | 无阻塞级缺口时为 `true`；`blocking_gaps_count > 0` 时必须为 `false` |
| `mind_map_files_scanned` | 实际检索/阅读过的 JSON 文件相对路径列表 |
| `keywords_used` | 用于检索脑图的关键词列表 |
| `*_count` | 必须与下文「改动清单」统计一致 |

---

## 1. 需求改动理解

### 1.1 改动背景

（简述本次需求背景、所属业务域、与存量功能关系）

### 1.2 改动点列表

| CHG-ID | 改动类型 | 需求摘要 | 来源 | 关键词 |
|--------|----------|----------|------|--------|
| CHG-001 | 新增/修改/删除/规则变更 | | 原文/推断 | |

### 1.3 范围说明

**In scope**：（本次分析覆盖的范围）

**Out of scope**：（明确不纳入脑图调整的内容）

---

## 2. 脑图检索记录

（说明如何在大文件中定位：用了哪些关键词、命中哪些文件、未命中时如何扩大检索）

| 关键词 | 命中文件 | 命中节点路径（示例） | 备注 |
|--------|----------|----------------------|------|
| | `测试脑图/xxx.json` | 根 > … > 叶子 | |

---

## 3. 改动清单（核心）

对每条 CHG-ID 给出脑图维度的操作建议：

| CHG-ID | 判定 | 脑图路径 | 现节点描述（摘录） | 操作建议 | 优先级 |
|--------|------|----------|-------------------|----------|--------|
| CHG-001 | 新增/修改/删除/无需改动/待确认 | `测试脑图/3263.json > A > B > C` 或「无」 | | 具体维护动作 | P0/P1/P2 |

**判定说明**：

- **无需改动**：现有节点已满足需求
- **修改**：更新 `biz_name` 文案、补充分支、调整预期结果描述
- **新增**：在指定父节点下增加子节点（写明建议节点名）
- **删除/废弃**：标记应移除或不再执行的旧分支
- **待确认**：已给默认建议，需人工复核

### 3.1 建议新增节点（汇总）

| 父节点路径 | 建议新增 biz_name | 关联 CHG-ID | 说明 |
|------------|-------------------|-------------|------|

### 3.2 建议修改节点（汇总）

| 脑图路径 | 现文案 | 建议文案 | 关联 CHG-ID |
|----------|--------|----------|-------------|

### 3.3 建议删除/废弃节点（汇总）

| 脑图路径 | 现文案 | 原因 | 关联 CHG-ID |
|----------|--------|------|-------------|

---

## 4. 缺口与待确认项

> **不向用户提问**；以下供维护人员线下确认。

### 4.1 阻塞级缺口

（导致 `ready_for_maintain=false` 的问题：需求矛盾、无法定位业务域、脑图文件缺失等）

| GAP-ID | 描述 | 影响 | 建议处理方式 |
|--------|------|------|--------------|

### 4.2 非阻塞级待确认

| Q-ID | 问题 | 默认假设 | 关联 CHG-ID |
|------|------|----------|-------------|

---

## 5. 维护执行建议

1. （在 QA 平台操作的推荐顺序：先改哪棵树、哪个父节点）
2. （与功能测试用例 / 自动化案例的衔接说明，若有）
3. （回归建议：改动后应重点复测的分支）

---

## 附录 A：完整路径索引（可选）

（若改动项较多，可按文件列出所有涉及路径，便于批量维护）

```
测试脑图/3263.json
  └─ 大陆资金
      └─ …
```
